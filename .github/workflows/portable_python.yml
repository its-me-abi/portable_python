name: Build Portable Python for Linux

on:
  workflow_dispatch:  # 👈 This adds the "Run workflow" button in Actions tab

jobs:
  build-portable-python:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential wget libssl-dev zlib1g-dev \
              libncurses5-dev libncursesw5-dev libreadline-dev libsqlite3-dev \
              libgdbm-dev libdb5.3-dev libbz2-dev libexpat1-dev liblzma-dev tk-dev

      - name: Download Python source
        run: |
          PY_VER=3.11.9
          wget https://www.python.org/ftp/python/$PY_VER/Python-$PY_VER.tgz
          tar xf Python-$PY_VER.tgz

      - name: Build Portable Python
        run: |
          cd Python-*
          ./configure --prefix=$PWD/../python-portable --enable-optimizations
          make -j$(nproc)
          make install
          cd ..
          ls -R python-portable

      - name: Package portable Python
        run: |
          tar czf python-portable.tar.gz python-portable

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-portable-linux
          path: python-portable.tar.gz
